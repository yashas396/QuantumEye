[project]
name = "quantum-eye"

[build]
builder = "nixpacks"

[deploy]
startCommand = "python build.py && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
healthcheckPath = "/api/status"
healthcheckTimeout = 300
